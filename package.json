{
  "name": "create-users",
  "version": "1.0.0",
  "description": "Node.js application for user creation, registration, and data persistence.",
  "type": "module",
  "main": "./src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch ./src/server.js",
    "postinstall": "prisma generate"
  },
  "author": "Alean Rocha",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^5.19.1"
  }
}
